---
phase: 02-edm-and-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/edm/edm-types.ts
  - packages/core/src/edm/edm-entity-type.ts
  - packages/core/src/edm/edm-entity-set.ts
  - packages/core/src/edm/edm-registry.ts
  - packages/core/src/edm/index.ts
  - packages/core/src/interfaces/edm-deriver.interface.ts
  - packages/core/src/interfaces/query-translator.interface.ts
  - packages/core/src/interfaces/index.ts
  - packages/core/src/decorators/metadata-keys.ts
  - packages/core/src/decorators/edm-type.decorator.ts
  - packages/core/src/decorators/odata-exclude.decorator.ts
  - packages/core/src/decorators/odata-entity-set.decorator.ts
  - packages/core/src/decorators/odata-key.decorator.ts
  - packages/core/src/decorators/odata-view.decorator.ts
  - packages/core/src/decorators/index.ts
  - packages/core/src/index.ts
  - packages/core/src/edm/edm-registry.spec.ts
  - packages/core/src/decorators/decorators.spec.ts
autonomous: true
requirements:
  - PKG-01
  - PKG-03
  - PKG-04

must_haves:
  truths:
    - 'EDM type system exists as discriminated unions in @nestjs-odata/core'
    - 'Adapter seam interfaces (IEdmDeriver, IQueryTranslator) are exported from core'
    - 'All OData decorators use reflect-metadata only — zero TypeORM imports'
    - 'EdmRegistry stores entity types and entity sets as a NestJS injectable singleton'
    - 'Virtual view concept is defined in core types'
  artifacts:
    - path: 'packages/core/src/edm/edm-types.ts'
      provides: 'EdmPrimitiveType union, EdmProperty, EdmNavigationProperty'
      contains: 'EdmPrimitiveType'
    - path: 'packages/core/src/interfaces/edm-deriver.interface.ts'
      provides: 'IEdmDeriver adapter interface'
      exports: ['IEdmDeriver']
    - path: 'packages/core/src/interfaces/query-translator.interface.ts'
      provides: 'IQueryTranslator adapter interface'
      exports: ['IQueryTranslator']
    - path: 'packages/core/src/edm/edm-registry.ts'
      provides: 'EdmRegistry injectable singleton'
      exports: ['EdmRegistry']
    - path: 'packages/core/src/decorators/edm-type.decorator.ts'
      provides: '@EdmType() property decorator'
      exports: ['EdmType']
    - path: 'packages/core/src/decorators/odata-exclude.decorator.ts'
      provides: '@ODataExclude() property decorator'
      exports: ['ODataExclude']
    - path: 'packages/core/src/decorators/odata-entity-set.decorator.ts'
      provides: '@ODataEntitySet() class decorator'
      exports: ['ODataEntitySet']
  key_links:
    - from: 'packages/core/src/index.ts'
      to: 'packages/core/src/edm/index.ts'
      via: 're-export'
      pattern: 'export.*from.*edm'
    - from: 'packages/core/src/index.ts'
      to: 'packages/core/src/interfaces/index.ts'
      via: 're-export'
      pattern: 'export.*from.*interfaces'
    - from: 'packages/core/src/index.ts'
      to: 'packages/core/src/decorators/index.ts'
      via: 're-export'
      pattern: 'export.*from.*decorators'
---

<objective>
Define all EDM data types, adapter interfaces, OData decorators, and the EdmRegistry in `@nestjs-odata/core`. This is the foundational contract layer that every subsequent plan depends on.

Purpose: Establish the type system and interfaces that the NestJS module (Plan 02), TypeORM deriver (Plan 03), and CSDL builder (Plan 04) will implement against. No downstream plan can start without these contracts.
Output: EDM types, IEdmDeriver/IQueryTranslator interfaces, all OData decorators, EdmRegistry singleton, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-edm-and-metadata/02-CONTEXT.md
@.planning/phases/02-edm-and-metadata/02-RESEARCH.md

<interfaces>
<!-- Existing discriminated union pattern to follow -->
From packages/core/src/parser/ast.ts:
```typescript
export type BinaryOperator = 'eq' | 'ne' | 'lt' | 'le' | 'gt' | 'ge' | ...
export interface BinaryExprNode {
  readonly kind: 'BinaryExpr'
  readonly operator: BinaryOperator
  readonly left: FilterNode
  readonly right: FilterNode
}
```

From packages/core/src/index.ts:

```typescript
export const VERSION = '0.0.1'
export * from './parser/index.js'
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EDM type system, adapter interfaces, and EdmRegistry</name>
  <files>
    packages/core/src/edm/edm-types.ts
    packages/core/src/edm/edm-entity-type.ts
    packages/core/src/edm/edm-entity-set.ts
    packages/core/src/edm/edm-registry.ts
    packages/core/src/edm/edm-registry.spec.ts
    packages/core/src/edm/index.ts
    packages/core/src/interfaces/edm-deriver.interface.ts
    packages/core/src/interfaces/query-translator.interface.ts
    packages/core/src/interfaces/index.ts
  </files>
  <read_first>
    packages/core/src/parser/ast.ts
    packages/core/src/index.ts
    packages/core/package.json
  </read_first>
  <behavior>
    - Test 1: EdmRegistry.register() stores an EdmEntityType and EdmEntitySet; getEntityType('Product') returns it
    - Test 2: EdmRegistry.getEntityTypes() returns a ReadonlyMap with all registered types
    - Test 3: EdmRegistry.getEntitySets() returns a ReadonlyMap with all registered sets
    - Test 4: EdmRegistry.register() with duplicate name throws error (fail-fast on misconfiguration)
    - Test 5: EdmRegistry.getEntityType('Unknown') returns undefined
  </behavior>
  <action>
    Create the EDM type system following the discriminated union pattern from `ast.ts` (per D-01, D-02, D-22, D-23, PKG-01, PKG-03, PKG-04):

    **packages/core/src/edm/edm-types.ts:**
    - `EdmPrimitiveType` string literal union: `'Edm.Binary' | 'Edm.Boolean' | 'Edm.Byte' | 'Edm.Date' | 'Edm.DateTimeOffset' | 'Edm.Decimal' | 'Edm.Double' | 'Edm.Guid' | 'Edm.Int16' | 'Edm.Int32' | 'Edm.Int64' | 'Edm.SByte' | 'Edm.Single' | 'Edm.String' | 'Edm.TimeOfDay'`
    - `EdmProperty` interface: `{ readonly name: string; readonly type: EdmPrimitiveType; readonly nullable: boolean; readonly precision?: number; readonly scale?: number; readonly maxLength?: number }`
    - `EdmNavigationProperty` interface: `{ readonly name: string; readonly type: string; readonly nullable: boolean; readonly isCollection: boolean; readonly partner?: string }`
    - `UnmappedTypeStrategy` type: `'skip' | 'string-fallback' | 'error'`

    **packages/core/src/edm/edm-entity-type.ts:**
    - `EdmEntityType` interface: `{ readonly name: string; readonly namespace: string; readonly properties: readonly EdmProperty[]; readonly navigationProperties: readonly EdmNavigationProperty[]; readonly keyProperties: readonly string[]; readonly isReadOnly: boolean }`

    **packages/core/src/edm/edm-entity-set.ts:**
    - `EdmEntitySet` interface: `{ readonly name: string; readonly entityTypeName: string; readonly namespace: string; readonly isReadOnly: boolean }`
    - `EdmEntityConfig` interface: `{ readonly entityTypeName: string; readonly entitySetName: string; readonly properties: readonly EdmProperty[]; readonly navigationProperties: readonly EdmNavigationProperty[]; readonly keyProperties: readonly string[]; readonly isReadOnly: boolean }`
    - `EdmVirtualView` interface (per D-22, D-23): `{ readonly name: string; readonly sourceEntityTypeName: string; readonly exposedProperties: readonly string[]; readonly entitySetName: string }`

    **packages/core/src/edm/edm-registry.ts:**
    - `@Injectable()` class `EdmRegistry` with:
      - `private readonly entityTypes = new Map<string, EdmEntityType>()`
      - `private readonly entitySets = new Map<string, EdmEntitySet>()`
      - `register(entityType: EdmEntityType, entitySet: EdmEntitySet): void` — throws if duplicate name
      - `getEntityType(name: string): EdmEntityType | undefined`
      - `getEntityTypes(): ReadonlyMap<string, EdmEntityType>`
      - `getEntitySets(): ReadonlyMap<string, EdmEntitySet>`
      - `getEntitySet(name: string): EdmEntitySet | undefined`

    **packages/core/src/interfaces/edm-deriver.interface.ts** (per D-01, D-02, PKG-03):
    - `IEdmDeriver` interface: `{ deriveEntityTypes(entityClasses: Function[]): EdmEntityConfig[] }`
    - Export injection token: `export const EDM_DERIVER = Symbol('EDM_DERIVER')`

    **packages/core/src/interfaces/query-translator.interface.ts** (per D-01, PKG-03):
    - `IQueryTranslator` interface: `{ translate(query: unknown, entityType: EdmEntityType): unknown }` (placeholder — Query AST types already exist in parser; full signature defined in Phase 3)
    - Export injection token: `export const QUERY_TRANSLATOR = Symbol('QUERY_TRANSLATOR')`

    **packages/core/src/interfaces/index.ts:** Re-export both interfaces and tokens.
    **packages/core/src/edm/index.ts:** Re-export all EDM types and EdmRegistry.

    CRITICAL: Zero imports from `typeorm` anywhere in packages/core/. The `EdmEntityConfig` uses only generic types. This satisfies PKG-01.

  </action>
  <verify>
    <automated>cd /Users/carlosber/repos/nestjs-odata && pnpm --filter @nestjs-odata/core test -- --reporter=verbose edm-registry</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/edm/edm-types.ts contains `export type EdmPrimitiveType =`
    - packages/core/src/edm/edm-types.ts contains `Edm.DateTimeOffset` (NOT Edm.DateTime)
    - packages/core/src/edm/edm-entity-type.ts contains `export interface EdmEntityType`
    - packages/core/src/edm/edm-entity-set.ts contains `export interface EdmEntityConfig`
    - packages/core/src/edm/edm-entity-set.ts contains `export interface EdmVirtualView`
    - packages/core/src/edm/edm-registry.ts contains `@Injectable()` and `export class EdmRegistry`
    - packages/core/src/interfaces/edm-deriver.interface.ts contains `export interface IEdmDeriver`
    - packages/core/src/interfaces/query-translator.interface.ts contains `export interface IQueryTranslator`
    - grep -r "typeorm" packages/core/src/ returns zero matches (PKG-01)
    - edm-registry.spec.ts exits 0
  </acceptance_criteria>
  <done>All EDM types, interfaces, and EdmRegistry are defined and tested. Zero TypeORM imports in core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OData decorators with reflect-metadata</name>
  <files>
    packages/core/src/decorators/metadata-keys.ts
    packages/core/src/decorators/edm-type.decorator.ts
    packages/core/src/decorators/odata-exclude.decorator.ts
    packages/core/src/decorators/odata-entity-set.decorator.ts
    packages/core/src/decorators/odata-key.decorator.ts
    packages/core/src/decorators/odata-view.decorator.ts
    packages/core/src/decorators/index.ts
    packages/core/src/decorators/decorators.spec.ts
    packages/core/src/index.ts
  </files>
  <read_first>
    packages/core/src/index.ts
    packages/core/src/edm/edm-types.ts
  </read_first>
  <behavior>
    - Test 1: @EdmType({ type: 'Edm.Decimal', precision: 10, scale: 2 }) on a property stores metadata retrievable via Reflect.getMetadata(EDM_TYPE_KEY, Target)
    - Test 2: @ODataExclude() on a property stores the property name in the exclude set
    - Test 3: @ODataEntitySet('CustomProducts') on a class stores the custom name retrievable via Reflect.getMetadata(ODATA_ENTITY_SET_KEY, Target)
    - Test 4: @ODataKey() on a property stores it in the key property list
    - Test 5: Class with no @ODataEntitySet returns undefined from Reflect.getMetadata (auto-pluralize fallback)
    - Test 6: @ODataView({ sourceEntity: 'Product', exposedProperties: ['id', 'name', 'price'] }) stores virtual view metadata
    - Test 7: Multiple @EdmType decorators on different properties of same class are all retrievable
  </behavior>
  <action>
    Create all OData decorators in `packages/core/src/decorators/` (per D-12, D-13, D-14, D-15, D-18, D-19, D-22):

    **packages/core/src/decorators/metadata-keys.ts:**
    ```typescript
    export const EDM_TYPE_KEY = Symbol('nestjs-odata:edm-type')
    export const ODATA_EXCLUDE_KEY = Symbol('nestjs-odata:odata-exclude')
    export const ODATA_ENTITY_SET_KEY = Symbol('nestjs-odata:entity-set')
    export const ODATA_KEY_KEY = Symbol('nestjs-odata:odata-key')
    export const ODATA_VIEW_KEY = Symbol('nestjs-odata:odata-view')
    ```

    **packages/core/src/decorators/edm-type.decorator.ts** (per D-12):
    - Export `EdmTypeOptions` interface: `{ type: string; precision?: number; scale?: number }`
    - Export `EdmType(options: EdmTypeOptions): PropertyDecorator` function
    - Uses `Reflect.defineMetadata(EDM_TYPE_KEY, ...)` on `target.constructor`
    - Stores as `Record<string | symbol, EdmTypeOptions>` (property name → options)
    - Helper export: `getEdmTypeOverrides(target: Function): Record<string, EdmTypeOptions>` reads metadata

    **packages/core/src/decorators/odata-exclude.decorator.ts** (per D-13):
    - Export `ODataExclude(): PropertyDecorator`
    - Stores as `Set<string | symbol>` on `target.constructor` using `ODATA_EXCLUDE_KEY`
    - Helper export: `getExcludedProperties(target: Function): Set<string>`

    **packages/core/src/decorators/odata-entity-set.decorator.ts** (per D-15):
    - Export `ODataEntitySet(name: string): ClassDecorator`
    - Stores name on the class using `ODATA_ENTITY_SET_KEY`
    - Helper export: `getEntitySetName(target: Function): string | undefined`

    **packages/core/src/decorators/odata-key.decorator.ts:**
    - Export `ODataKey(): PropertyDecorator`
    - Stores as `string[]` on `target.constructor` using `ODATA_KEY_KEY`
    - Helper export: `getKeyProperties(target: Function): string[]`

    **packages/core/src/decorators/odata-view.decorator.ts** (per D-22, D-23):
    - Export `ODataViewOptions` interface: `{ sourceEntity: string; exposedProperties: string[]; entitySetName?: string }`
    - Export `ODataView(options: ODataViewOptions): ClassDecorator`
    - Stores on class using `ODATA_VIEW_KEY`
    - Helper export: `getODataViewOptions(target: Function): ODataViewOptions | undefined`

    **packages/core/src/decorators/index.ts:** Re-export all decorators, helpers, and metadata keys.

    **packages/core/src/index.ts:** Update to also export `./edm/index.js`, `./interfaces/index.js`, `./decorators/index.js`.

    CRITICAL: All decorators import ONLY from `reflect-metadata` and local files. Zero imports from `typeorm`, `@nestjs/common`, or any ORM. Per D-19, decorators are pure metadata annotations.

  </action>
  <verify>
    <automated>cd /Users/carlosber/repos/nestjs-odata && pnpm --filter @nestjs-odata/core test -- --reporter=verbose decorators</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/decorators/metadata-keys.ts contains `export const EDM_TYPE_KEY = Symbol(`
    - packages/core/src/decorators/edm-type.decorator.ts contains `export function EdmType(`
    - packages/core/src/decorators/odata-exclude.decorator.ts contains `export function ODataExclude(`
    - packages/core/src/decorators/odata-entity-set.decorator.ts contains `export function ODataEntitySet(`
    - packages/core/src/decorators/odata-key.decorator.ts contains `export function ODataKey(`
    - packages/core/src/decorators/odata-view.decorator.ts contains `export function ODataView(`
    - packages/core/src/index.ts contains `export * from './decorators/index.js'`
    - packages/core/src/index.ts contains `export * from './edm/index.js'`
    - packages/core/src/index.ts contains `export * from './interfaces/index.js'`
    - grep -r "from 'typeorm'" packages/core/src/decorators/ returns zero matches
    - grep -r "from '@nestjs'" packages/core/src/decorators/ returns zero matches
    - decorators.spec.ts exits 0
  </acceptance_criteria>
  <done>All OData decorators are defined, tested, and exported from @nestjs-odata/core with zero ORM imports.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                                         |
| ------------------ | --------------------------------------------------------------------------------------------------- |
| Decorator metadata | Metadata stored via reflect-metadata on entity classes — consumed at init time, not at request time |

## STRIDE Threat Register

| Threat ID | Category                   | Component              | Disposition | Mitigation Plan                                                              |
| --------- | -------------------------- | ---------------------- | ----------- | ---------------------------------------------------------------------------- |
| T-02-01   | T (Tampering)              | EdmRegistry.register() | mitigate    | Throw on duplicate registration to prevent silent override of entity types   |
| T-02-02   | I (Information Disclosure) | Decorator metadata     | accept      | Decorator metadata is internal to the server process; not exposed to clients |

</threat_model>

<verification>
1. `pnpm --filter @nestjs-odata/core test` passes with all EDM and decorator tests green
2. `grep -r "typeorm" packages/core/src/` returns zero matches
3. `pnpm --filter @nestjs-odata/core typecheck` passes
</verification>

<success_criteria>

- EdmPrimitiveType union includes all 15 OData v4 primitive types (NO Edm.DateTime)
- IEdmDeriver and IQueryTranslator interfaces exported from core
- All 5 decorators (@EdmType, @ODataExclude, @ODataEntitySet, @ODataKey, @ODataView) work with reflect-metadata
- EdmRegistry stores and retrieves entity types and sets correctly
- Zero TypeORM imports in packages/core/src/
  </success_criteria>

<output>
After completion, create `.planning/phases/02-edm-and-metadata/02-01-SUMMARY.md`
</output>
