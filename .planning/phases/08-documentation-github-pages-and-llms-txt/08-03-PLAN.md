---
phase: 08-documentation-github-pages-and-llms-txt
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - .claude/skills/doc-guardian/SKILL.md
  - .claude/skills/doc-guardian/rules/doc-coverage.md
autonomous: true
requirements:
  - DOC-SKILL
must_haves:
  truths:
    - 'Doc-guardian skill exists and can be invoked by Claude Code'
    - 'Skill includes a mapping from source file paths to affected doc files'
    - 'Skill instructions explain how to read diffs and determine which docs need updates'
    - 'Full docs build succeeds with all content from Plans 01 and 02 combined'
  artifacts:
    - path: '.claude/skills/doc-guardian/SKILL.md'
      provides: 'Skill definition with frontmatter and instructions'
      min_lines: 50
      contains: 'doc-guardian'
    - path: '.claude/skills/doc-guardian/rules/doc-coverage.md'
      provides: 'Source file to doc file mapping'
      min_lines: 30
      contains: 'filter-visitor'
  key_links:
    - from: '.claude/skills/doc-guardian/SKILL.md'
      to: '.claude/skills/doc-guardian/rules/doc-coverage.md'
      via: 'SKILL.md references rules/doc-coverage.md for the mapping'
      pattern: 'doc-coverage'
---

<objective>
Create the doc-guardian project skill (per D-03) that evaluates code diffs and updates affected documentation, then verify the complete docs build with all infrastructure and content from Plans 01 and 02.

Purpose: Ensure documentation never drifts from implementation in future phases. The skill is invoked after phase execution to automatically update affected docs.
Output: `.claude/skills/doc-guardian/` directory with SKILL.md and rules, plus verified full build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-documentation-github-pages-and-llms-txt/08-RESEARCH.md (Pattern 4: Doc-Guardian Skill)
@.planning/phases/08-documentation-github-pages-and-llms-txt/08-01-SUMMARY.md
@.planning/phases/08-documentation-github-pages-and-llms-txt/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create doc-guardian skill with source-to-doc mapping</name>
  <files>.claude/skills/doc-guardian/SKILL.md, .claude/skills/doc-guardian/rules/doc-coverage.md</files>
  <read_first>
    - .planning/phases/08-documentation-github-pages-and-llms-txt/08-RESEARCH.md (Pattern 4: Doc-Guardian Skill section)
    - packages/core/src/index.ts (public API surface)
    - packages/typeorm/src/index.ts (adapter public API surface)
    - docs/.vitepress/config.mts (current sidebar structure)
  </read_first>
  <action>
1. Create directory `.claude/skills/doc-guardian/rules/`.

2. Create `.claude/skills/doc-guardian/SKILL.md` as a Claude Code skill file. The file MUST have YAML frontmatter (delimited by triple-dash lines) with these exact fields:
   - name: doc-guardian
   - description: "Evaluates whether code changes require documentation updates and applies them. Invoke after a phase plan completes, when diffs touch packages/core or packages/typeorm source files. Reads the git diff, checks the source-to-doc mapping in rules/doc-coverage.md, reads affected doc files, and updates them to reflect the code changes."

   After the frontmatter, include a markdown body with these sections:

   **# Doc Guardian** heading

   **## When to Use** section: After completing a phase plan that modifies files in packages/core/src/, packages/typeorm/src/, or apps/test-app/src/.

   **## How It Works** section with 4 numbered steps:
   - Step 1: Read the diff against the base branch (default: origin/main). Show the command `git diff origin/main --name-only -- packages/core packages/typeorm`. Note: accept an optional base ref argument to override.
   - Step 2: Map changed files to affected docs using the mapping in rules/doc-coverage.md.
   - Step 3: For each affected doc file: (a) read current doc content, (b) read changed source files to understand what changed, (c) update doc to reflect new API surface/config/behavior, (d) verify code examples still match implementation.
   - Step 4: Verify the build after making changes by running `cd docs && pnpm build`.

   **## Rules** section with these bullet points:
   - Do NOT regenerate docs from scratch, update only what changed
   - Do NOT modify auto-generated TypeDoc output (files in docs/api/generated/)
   - Every code example must be verifiable against the actual source
   - If a decorator signature changed, update both docs/api/decorators.md and any guide that references it
   - If a new feature was added, add it to the relevant guide AND the API reference
   - If unsure whether a change requires doc updates, err on the side of updating

   **## Source Files** section: Reference @.claude/skills/doc-guardian/rules/doc-coverage.md

   The file should be at least 50 lines.

3. Create `.claude/skills/doc-guardian/rules/doc-coverage.md` with a comprehensive source-to-doc mapping. Include 4 tables:

   **## Core Package (packages/core/src/)** table with columns: Source Pattern | Affected Docs | What to Check. Include rows for:
   - decorators/\*.ts -> docs/api/decorators.md
   - decorators/odata-controller.decorator.ts -> docs/guide/getting-started.md, docs/examples/custom-controller.md
   - odata.module.ts -> docs/api/module.md, docs/guide/configuration.md
   - interfaces/module-options.interface.ts -> docs/api/module.md, docs/guide/configuration.md, docs/guide/security.md
   - pipes/odata-query.pipe.ts -> docs/guide/query-options.md
   - interceptors/odata-response.interceptor.ts -> docs/guide/query-options.md
   - filters/odata-exception.filter.ts -> docs/guide/security.md
   - batch/\*\* -> docs/guide/batch.md
   - index.ts -> All API docs

   **## TypeORM Adapter (packages/typeorm/src/)** table with rows for:
   - translator/filter-visitor.ts -> docs/guide/query-options.md, docs/guide/filter-functions.md
   - translator/select-visitor.ts -> docs/guide/query-options.md
   - translator/orderby-visitor.ts -> docs/guide/query-options.md
   - translator/expand-visitor.ts -> docs/guide/expand.md
   - auto-handler.ts -> docs/guide/crud.md, docs/examples/basic-crud.md
   - deriver/typeorm-edm-deriver.ts -> docs/guide/getting-started.md
   - index.ts -> docs/api/module.md

   **## Test App (apps/test-app/src/)** table with rows for:
   - \*.controller.ts -> docs/examples/basic-crud.md, docs/examples/custom-controller.md
   - \*.module.ts -> docs/guide/getting-started.md

   **## Cross-Cutting Changes** table with rows for:
   - New decorator added -> docs/api/decorators.md + relevant guide
   - New config option -> docs/api/module.md, docs/guide/configuration.md
   - New query option -> docs/guide/query-options.md + dedicated guide if complex
   - Security limit added -> docs/guide/security.md, docs/guide/configuration.md
   - Response format change -> docs/guide/query-options.md
   - New filter function -> docs/guide/filter-functions.md

   The file should be at least 30 lines.

4. Verify the skill directory structure:
   ```bash
   ls -la .claude/skills/doc-guardian/
   ls -la .claude/skills/doc-guardian/rules/
   ```
     </action>
     <verify>
       <automated>cd /Users/carlosber/repos/nestjs-odata && test -f .claude/skills/doc-guardian/SKILL.md && test -f .claude/skills/doc-guardian/rules/doc-coverage.md && echo "Skill files exist" && wc -l .claude/skills/doc-guardian/SKILL.md | awk '{if ($1 >= 50) print "SKILL.md OK: " $1 " lines"; else {print "FAIL: only " $1 " lines"; exit 1}}' && wc -l .claude/skills/doc-guardian/rules/doc-coverage.md | awk '{if ($1 >= 30) print "doc-coverage.md OK: " $1 " lines"; else {print "FAIL: only " $1 " lines"; exit 1}}'</automated>
     </verify>
     <acceptance_criteria>
       - File `.claude/skills/doc-guardian/SKILL.md` exists with at least 50 lines
       - File `.claude/skills/doc-guardian/rules/doc-coverage.md` exists with at least 30 lines
       - `grep "name: doc-guardian" .claude/skills/doc-guardian/SKILL.md` returns a match (frontmatter present)
       - `grep "description:" .claude/skills/doc-guardian/SKILL.md` returns a match (frontmatter description present)
       - `grep "filter-visitor" .claude/skills/doc-guardian/rules/doc-coverage.md` returns a match (filter-visitor mapping present)
       - `grep "decorators" .claude/skills/doc-guardian/rules/doc-coverage.md` returns a match (decorator mapping present)
       - `grep "odata.module" .claude/skills/doc-guardian/rules/doc-coverage.md` returns a match (module mapping present)
       - `grep "origin/main" .claude/skills/doc-guardian/SKILL.md` returns a match (default diff base documented)
       - `grep "doc-coverage" .claude/skills/doc-guardian/SKILL.md` returns a match (references rules file)
     </acceptance_criteria>
     <done>Doc-guardian skill created with SKILL.md frontmatter, step-by-step instructions, and comprehensive source-to-doc mapping in rules/doc-coverage.md. Skill defaults to diffing against origin/main and maps all major source file patterns to their affected documentation files.</done>
   </task>

<task type="auto">
  <name>Task 2: Verify complete docs build and llms.txt output</name>
  <files></files>
  <read_first>
    - docs/package.json (verify build script)
    - docs/.vitepress/config.mts (verify all plugins wired)
  </read_first>
  <action>
Run the full docs build pipeline and verify all expected outputs:

1. Build packages first (TypeDoc needs compiled core):

   ```bash
   cd /Users/carlosber/repos/nestjs-odata && pnpm turbo build
   ```

2. Run docs build (typedoc + vitepress):

   ```bash
   cd /Users/carlosber/repos/nestjs-odata/docs && pnpm build
   ```

3. Verify llms.txt output:

   ```bash
   ls -la docs/.vitepress/dist/llms.txt
   ls -la docs/.vitepress/dist/llms-full.txt
   ```

4. Verify TypeDoc generated output:

   ```bash
   ls docs/api/generated/typedoc-sidebar.json
   ls docs/api/generated/index.md
   ```

5. Verify key docs pages exist in build output:

   ```bash
   ls docs/.vitepress/dist/guide/filter-functions.html
   ls docs/.vitepress/dist/guide/getting-started.html
   ls docs/.vitepress/dist/api/decorators.html
   ```

6. If any step fails, diagnose and fix the issue. Common fixes:
   - If typedoc fails: check docs/typedoc.json entryPoint path
   - If vitepress fails: check config.mts imports
   - If llms.txt missing: check vite plugins array in config.mts
   - If filter-functions.html missing: check sidebar config and file exists

Do NOT modify source code files. This task is verification only. If issues are found, fix only docs infrastructure files (config, package.json) or flag for prior plans.
</action>
<verify>
<automated>cd /Users/carlosber/repos/nestjs-odata/docs && pnpm build && test -f .vitepress/dist/llms.txt && test -f .vitepress/dist/guide/filter-functions.html && echo "Full build verified"</automated>
</verify>
<acceptance_criteria> - `pnpm build` in docs/ exits with code 0 - File `docs/.vitepress/dist/llms.txt` exists (llms plugin working) - File `docs/.vitepress/dist/llms-full.txt` exists (full text version generated) - File `docs/.vitepress/dist/guide/filter-functions.html` exists (new guide rendered) - File `docs/.vitepress/dist/guide/getting-started.html` exists (existing guides rendered) - File `docs/.vitepress/dist/api/decorators.html` exists (API reference rendered) - File `docs/api/generated/typedoc-sidebar.json` exists (TypeDoc generated sidebar)
</acceptance_criteria>
<done>Complete docs build pipeline verified: TypeDoc generates API reference, VitePress builds all pages including new filter-functions guide, llms.txt and llms-full.txt generated in build output, all existing pages render correctly.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                             |
| -------- | --------------------------------------- |
| (none)   | Skill files and build verification only |

## STRIDE Threat Register

| Threat ID | Category  | Component          | Disposition | Mitigation Plan                                                                          |
| --------- | --------- | ------------------ | ----------- | ---------------------------------------------------------------------------------------- |
| T-08-04   | Tampering | doc-guardian skill | accept      | Skill is a development tool only; committed to repo; reviewed in PRs like any other code |

</threat_model>

<verification>
1. `.claude/skills/doc-guardian/SKILL.md` exists with valid frontmatter
2. `.claude/skills/doc-guardian/rules/doc-coverage.md` has comprehensive source-to-doc mapping
3. Full docs build (typedoc + vitepress) passes
4. llms.txt present in build output
5. All doc pages render as HTML in dist/
</verification>

<success_criteria>

- Doc-guardian skill is a functioning Claude Code skill with frontmatter, instructions, and rules
- Source-to-doc mapping covers all major packages (core, typeorm, test-app)
- Full docs pipeline builds without errors
- llms.txt and llms-full.txt generated
- All guide, API, and example pages render
  </success_criteria>

<output>
After completion, create `.planning/phases/08-documentation-github-pages-and-llms-txt/08-03-SUMMARY.md`
</output>
