---
phase: 05-batch-security-and-v1-hardening
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - packages/core/package.json
  - packages/typeorm/package.json
autonomous: true
requirements: []

must_haves:
  truths:
    - 'publint passes with zero errors for both @nestjs-odata/core and @nestjs-odata/typeorm'
    - '@arethetypeswrong/cli reports no UntypedResolution or entrypoint issues for either package'
    - 'exports["."].types is the first key in both package.json exports objects'
    - 'packages/typeorm/package.json exports["."].types points to ./dist/index.d.mts (actual tsdown output)'
  artifacts:
    - path: 'packages/core/package.json'
      provides: 'Correct exports field ordering with types first'
      contains: '"types"'
    - path: 'packages/typeorm/package.json'
      provides: 'Correct exports with types pointing to actual .d.mts file'
      contains: 'index.d.mts'
---

<objective>
Fix package.json exports configuration for both packages so that publint and @arethetypeswrong/cli pass with zero errors. This closes the 2 gaps from Phase 5 verification.
</objective>

<threat_model>
No security implications — this is a packaging metadata fix only.
</threat_model>

<tasks>

<task id="1" name="Fix package.json exports for both packages" type="execute">

<read_first>

- packages/core/package.json
- packages/typeorm/package.json
- packages/core/dist/ (list files to confirm actual output filenames)
- packages/typeorm/dist/ (list files to confirm actual output filenames)
  </read_first>

<action>
**Gap 1: packages/typeorm/package.json**

The `exports["."].types` field points to `./dist/index.d.ts` which does not exist. tsdown produces `index.d.mts` and `index.d.cts`.

Fix:

1. Change `"types": "./dist/index.d.ts"` to `"types": "./dist/index.d.mts"` inside `exports["."]`
2. Move the `"types"` key to be the FIRST key in the `exports["."]` object (before `"import"` and `"require"`)
3. Also update the top-level `"types"` field to `"./dist/index.d.mts"` if it exists

**Gap 2: packages/core/package.json**

The `exports["."]` object has `"types"` as the last key. publint requires it first.

Fix:

1. Move the `"types"` key to be the FIRST key in the `exports["."]` object (before `"import"` and `"require"`)
2. Verify the types path is correct (should match actual tsdown output)

**Verification:**
After fixing both files, run:

```bash
cd packages/core && npx publint && npx attw --pack .
cd ../typeorm && npx publint && npx attw --pack .
```

Both must pass with zero errors.
</action>

<acceptance_criteria>

- packages/core/package.json exports["."] has "types" as the first key
- packages/typeorm/package.json exports["."] has "types" as the first key
- packages/typeorm/package.json exports["."].types contains "index.d.mts"
- packages/typeorm/package.json does NOT contain "index.d.ts" in exports
- publint exits 0 for both packages
- @arethetypeswrong/cli exits 0 for both packages (no UntypedResolution)
  </acceptance_criteria>

</task>

</tasks>

<verification>
- publint passes for both packages
- @arethetypeswrong/cli passes for both packages
- pnpm build still succeeds
</verification>
