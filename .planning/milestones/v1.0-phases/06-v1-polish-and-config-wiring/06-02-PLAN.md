---
phase: 06-v1-polish-and-config-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/typeorm/package.json
  - .claude/agents/odata-expert.md
autonomous: false
requirements:
  - SCAF-08

must_haves:
  truths:
    - 'pnpm install does not warn about unmet peer dependency for @nestjs-odata/core'
    - 'OData sub-agent answers 6 representative OData v4 spec questions without factual errors'
  artifacts:
    - path: 'packages/typeorm/package.json'
      provides: 'Correct peer dep range for @nestjs-odata/core'
      contains: '>=0.0.1'
    - path: '.claude/agents/odata-expert.md'
      provides: 'Validated sub-agent with confirmed correct spec knowledge'
  key_links:
    - from: 'packages/typeorm/package.json peerDependencies'
      to: '@nestjs-odata/core version 0.0.2'
      via: 'semver range >=0.0.1 satisfies 0.0.2'
      pattern: '"@nestjs-odata/core": ">=0.0.1"'
---

<objective>
Fix the peer dependency version mismatch (typeorm declares >=0.1.0 but core is at 0.0.2) and formally validate the odata-expert sub-agent against representative OASIS spec questions.

Purpose: The peer dep mismatch would cause install warnings for any consumer who installs both packages at their current versions. The sub-agent needs formal validation to satisfy SCAF-08.
Output: Corrected package.json peer dep range and a validated sub-agent with proof of correct answers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-v1-polish-and-config-wiring/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix peer dep version mismatch in @nestjs-odata/typeorm</name>
  <files>packages/typeorm/package.json</files>
  <read_first>
    - packages/typeorm/package.json (full file — find the peerDependencies section)
    - packages/core/package.json (find the current version field)
  </read_first>
  <action>
    Read packages/typeorm/package.json. In the `peerDependencies` section, find:
    ```json
    "@nestjs-odata/core": ">=0.1.0"
    ```
    Change it to:
    ```json
    "@nestjs-odata/core": ">=0.0.1"
    ```

    This change satisfies the constraint that core is currently at 0.0.2 (which satisfies >=0.0.1 but NOT >=0.1.0).

    Also read packages/core/package.json to confirm the version field shows "0.0.2" (if it shows a different version, adjust the peer dep range to >=0.0.{N-1} where N is the current patch version, always leaving room for the installed version to satisfy the range).

    Do NOT change any other fields in package.json. This is a single string change in the peerDependencies object.

    NOTE: Do NOT run `pnpm install` — the lock file is the concern of the CI pipeline. This is a manifest-only fix.

  </action>
  <verify>
    <automated>node -e "const p=require('./packages/typeorm/package.json'); const range=p.peerDependencies['@nestjs-odata/core']; const semver=require('semver'); const coreVersion=require('./packages/core/package.json').version; if(!semver.satisfies(coreVersion, range)) { console.error('FAIL: core version '+coreVersion+' does not satisfy range '+range); process.exit(1); } console.log('OK: '+coreVersion+' satisfies '+range);"</automated>
  </verify>
  <acceptance_criteria>
    - grep '"@nestjs-odata/core"' packages/typeorm/package.json shows a range that satisfies the current core version (0.0.2)
    - grep '"@nestjs-odata/core": ">=0.0.1"' packages/typeorm/package.json returns a match
    - packages/core/package.json version field is "0.0.2" (verify before editing to confirm assumption)
  </acceptance_criteria>
  <done>@nestjs-odata/typeorm peer dep for @nestjs-odata/core satisfies the currently published core version (0.0.2); no install warnings would occur for consumers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate odata-expert sub-agent against OASIS spec questions (SCAF-08)</name>
  <files>.claude/agents/odata-expert.md</files>
  <read_first>
    - .claude/agents/odata-expert.md (full file — review current knowledge areas before posing questions)
  </read_first>
  <action>
    Automated prep: Read .claude/agents/odata-expert.md in full to understand its current knowledge sections.
    Then pause for human verification (see how-to-verify below). If any answers are incorrect, update the relevant knowledge sections in .claude/agents/odata-expert.md with the correct spec information.
  </action>
  <verify>
    <automated>MISSING — this task requires human verification; see how-to-verify below</automated>
  </verify>
  <what-built>
    The odata-expert sub-agent was scaffolded in Phase 1 with references to 6 OASIS spec documents. It has documented knowledge across query options, CSDL, JSON format, $batch, security, and the ABNF grammar. This task validates its depth against concrete spec questions.
  </what-built>
  <how-to-verify>
    Invoke the odata-expert agent using `/odata-expert` (or `claude --agent odata-expert`) and ask it the following 6 questions. Score each answer as PASS (spec-correct) or FAIL (incorrect or missing citation):

    **Q1 — $filter operator precedence:**
    "In OData v4.01, does `a eq 1 or b eq 2 and c eq 3` evaluate `b eq 2 and c eq 3` first (and is the `and` tighter than `or`)? Cite the spec section."
    Expected answer: YES — `and` has higher precedence than `or`. Per Part 2 Section 5.1.1.x or the ABNF grammar.

    **Q2 — $expand with $filter inside expand:**
    "Is `$expand=Orders($filter=Status eq 'Open')` valid OData v4? If yes, what does it do?"
    Expected answer: YES — nested system query options in $expand are valid per Part 2 Section 5.1.3. The $filter inside $expand applies to the expanded collection.

    **Q3 — $batch changeset atomicity:**
    "If a changeset in a $batch request contains 3 operations and operation 2 fails, what must the server do with operations 1 and 3?"
    Expected answer: The entire changeset must be rolled back — operation 1 must be undone, operation 3 must not be applied. Per Part 1 Section 11.7.x.

    **Q4 — CSDL NavigationProperty:**
    "In CSDL XML, what attribute specifies the type of a NavigationProperty, and how is a collection navigation property (OneToMany) declared differently from a single-valued one (ManyToOne)?"
    Expected answer: The `Type` attribute. For collection: `Type="Collection(Namespace.TypeName)"`. For single: `Type="Namespace.TypeName"`. Per CSDL XML spec Section 8.x.

    **Q5 — OData error response format:**
    "What is the required JSON structure for an OData v4 error response? List the required top-level property and its required sub-properties."
    Expected answer: Top-level `error` object with required sub-properties `code` (string) and `message` (string). Optional: `target`, `details` array, `innererror`. Per JSON Format Section 9.

    **Q6 — maxLength for Edm.String:**
    "Can an OData service declare a maximum length constraint on an Edm.String property in CSDL? If so, what CSDL attribute is used?"
    Expected answer: YES — the `MaxLength` facet attribute on the Property element. Per CSDL XML Section 7.2.3 or similar.

    **Scoring:** 5/6 or 6/6 = PASS. 4/6 or below = FAIL — update the agent knowledge section for each failed question.

    If any answers are wrong, update `.claude/agents/odata-expert.md` with the correct information in the relevant knowledge section. Then re-run the failing questions to confirm the correction.

  </how-to-verify>
  <resume-signal>
    Type "validated: N/6 passed" where N is the number of correct answers (e.g., "validated: 6/6 passed").
    If any questions failed, describe which sections were updated in the agent file.
  </resume-signal>
  <acceptance_criteria>
    - 5 or more of the 6 questions answered correctly by the agent (human-verified)
    - Any failed questions have been corrected in .claude/agents/odata-expert.md
    - The agent file contains accurate answers for all 6 knowledge areas tested
  </acceptance_criteria>
  <done>OData sub-agent answers at least 5/6 representative OASIS spec questions correctly; SCAF-08 is satisfied.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                 |
| -------------------------------- | ------------------------------------------------------------------------------------------- |
| npm registry peer dep resolution | Peer dep version range must accurately reflect compatible versions to avoid silent failures |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                                                                                                  |
| --------- | ---------------------- | --------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------- |
| T-06-03   | Tampering              | package.json peer dep range | mitigate    | Fix range to >=0.0.1 so it accurately documents compatibility — incorrect ranges can cause incorrect dependency resolution       |
| T-06-04   | Information Disclosure | odata-expert sub-agent      | accept      | Agent provides spec guidance only; incorrect answers lead to implementation bugs not security issues — validated by human review |

</threat_model>

<verification>
After Task 1:
```bash
grep '"@nestjs-odata/core"' packages/typeorm/package.json
# Expected: ">=0.0.1" (not ">=0.1.0")
```

After Task 2 (human verified): 5+ of 6 spec questions answered correctly.
</verification>

<success_criteria>

- packages/typeorm/package.json peerDependencies["@nestjs-odata/core"] is ">=0.0.1" (satisfies current core 0.0.2)
- OData sub-agent achieves 5/6 or 6/6 score on the OASIS spec question set
- SCAF-08 is fully satisfied: the sub-agent is formally validated, not just scaffolded
  </success_criteria>

<output>
After completion, create `.planning/phases/06-v1-polish-and-config-wiring/06-02-SUMMARY.md`
</output>
