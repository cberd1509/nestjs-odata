---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/package.json
  - packages/typeorm/package.json
  - packages/core/README.md
  - packages/typeorm/README.md
  - .changeset/config.json
autonomous: true
requirements: []
must_haves:
  truths:
    - 'Both packages have version 1.0.0'
    - 'Both packages have repository field pointing to cberd1509/nestjs-odata'
    - 'Both packages have relevant keywords for npm discoverability'
    - 'Both packages have per-package README.md files'
    - 'CHANGELOG.md is removed from the files array in both packages'
    - 'Changeset config repo field points to cberd1509/nestjs-odata'
  artifacts:
    - path: 'packages/core/package.json'
      provides: 'Fixed npm metadata for core'
      contains: 'cberd1509/nestjs-odata'
    - path: 'packages/typeorm/package.json'
      provides: 'Fixed npm metadata for typeorm adapter'
      contains: 'cberd1509/nestjs-odata'
    - path: 'packages/core/README.md'
      provides: 'Per-package README for npm listing'
    - path: 'packages/typeorm/README.md'
      provides: 'Per-package README for npm listing'
    - path: '.changeset/config.json'
      provides: 'Fixed repo reference'
      contains: 'cberd1509/nestjs-odata'
  key_links: []
---

<objective>
Fix npm publish metadata for both @nestjs-odata/core and @nestjs-odata/typeorm packages so they display correctly on npmjs.com with proper repository links, keywords, version, and README content.

Purpose: Packages currently have placeholder versions (0.0.x), missing repository field, no keywords, no per-package READMEs (npm shows blank), CHANGELOG.md in files array (does not exist), and changeset config points to wrong repo.
Output: Both package.json files updated, per-package READMEs created, changeset config fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/core/package.json
@packages/typeorm/package.json
@.changeset/config.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json metadata for both packages</name>
  <files>packages/core/package.json, packages/typeorm/package.json, .changeset/config.json</files>
  <action>
For BOTH packages/core/package.json and packages/typeorm/package.json:

1. Set `"version": "1.0.0"`

2. Add repository field:

```json
"repository": {
  "type": "git",
  "url": "https://github.com/cberd1509/nestjs-odata.git",
  "directory": "packages/core"
}
```

(Use `"directory": "packages/typeorm"` for the typeorm package)

3. Add `"homepage": "https://github.com/cberd1509/nestjs-odata#readme"`

4. Add `"bugs": { "url": "https://github.com/cberd1509/nestjs-odata/issues" }`

5. Add keywords array:

- For core: `["nestjs", "odata", "odata-v4", "rest", "api", "edm", "metadata", "nestjs-module"]`
- For typeorm: `["nestjs", "odata", "odata-v4", "typeorm", "rest", "api", "edm", "orm-adapter"]`

6. Remove `"CHANGELOG.md"` from the `"files"` array in both packages (keep `"dist"` and `"README.md"`)

7. In .changeset/config.json, change `"repo": "your-org/nestjs-odata"` to `"repo": "cberd1509/nestjs-odata"`

Place the new fields after `"license"` for readability (repository, homepage, bugs, keywords).
</action>
<verify>
<automated>cd /Users/carlosber/repos/nestjs-odata && node -e "
const core = require('./packages/core/package.json');
const typeorm = require('./packages/typeorm/package.json');
const cs = require('./.changeset/config.json');
const assert = (c, m) => { if (!c) throw new Error(m); };
assert(core.version === '1.0.0', 'core version');
assert(typeorm.version === '1.0.0', 'typeorm version');
assert(core.repository?.url?.includes('cberd1509'), 'core repo');
assert(typeorm.repository?.url?.includes('cberd1509'), 'typeorm repo');
assert(core.keywords?.length > 0, 'core keywords');
assert(typeorm.keywords?.length > 0, 'typeorm keywords');
assert(!core.files.includes('CHANGELOG.md'), 'core no changelog');
assert(!typeorm.files.includes('CHANGELOG.md'), 'typeorm no changelog');
assert(cs.changelog[1].repo === 'cberd1509/nestjs-odata', 'changeset repo');
console.log('All metadata checks passed');
"</automated>
</verify>
<done>Both package.json files have version 1.0.0, repository/homepage/bugs pointing to cberd1509/nestjs-odata, keywords for npm discoverability, no CHANGELOG.md in files array. Changeset config repo is correct.</done>
</task>

<task type="auto">
  <name>Task 2: Create per-package README.md files</name>
  <files>packages/core/README.md, packages/typeorm/README.md</files>
  <action>
Create focused per-package READMEs that npm will display on the package page. These should be concise and package-specific, not a copy of the root README.

**packages/core/README.md:**

- Title: `@nestjs-odata/core`
- One-line description: Core OData v4 library for NestJS
- Badges: npm version, npm downloads, license (use shields.io pointing to @nestjs-odata/core)
- Brief features list (parser, EDM, decorators, module, interceptors, batch)
- Installation: `pnpm add @nestjs-odata/core @nestjs-odata/typeorm reflect-metadata`
- Short usage example (import ODataModule, register in NestJS module) — keep it to 15-20 lines of TypeScript max
- Link to full docs: https://nestjs-odata.github.io/nestjs-odata/
- Link to root repo: https://github.com/cberd1509/nestjs-odata
- License: MIT

**packages/typeorm/README.md:**

- Title: `@nestjs-odata/typeorm`
- One-line description: TypeORM adapter for @nestjs-odata/core
- Badges: npm version, npm downloads, license (use shields.io pointing to @nestjs-odata/typeorm)
- Brief features list (auto EDM derivation from TypeORM entities, query translation, CRUD operations)
- Installation: same as core
- Short usage example (TypeORM entity + ODataController extending TypeOrmODataController) — keep it to 15-20 lines max
- Link to full docs and root repo
- License: MIT

Keep both READMEs under 120 lines. These are npm landing pages, not full documentation.
</action>
<verify>
<automated>cd /Users/carlosber/repos/nestjs-odata && test -f packages/core/README.md && test -f packages/typeorm/README.md && echo "Both READMEs exist" && wc -l packages/core/README.md packages/typeorm/README.md</automated>
</verify>
<done>Both packages have README.md files that npm will display. Each is under 120 lines, package-specific, with badges, install instructions, usage example, and links to full docs.</done>
</task>

</tasks>

<verification>
- `node -e "..."` metadata validation script passes
- Both README.md files exist and are under 120 lines
- `pnpm build` still succeeds (metadata changes should not affect build)
</verification>

<success_criteria>

- npm publish dry-run would show correct metadata (version 1.0.0, repo, keywords, README)
- No CHANGELOG.md referenced in files arrays
- Changeset config points to correct GitHub repo
  </success_criteria>

<output>
After completion, create `.planning/quick/260408-ox4-fix-npm-publish-metadata-for-both-packag/260408-ox4-SUMMARY.md`
</output>
